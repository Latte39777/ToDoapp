import Login from "../features/auth/Login";

const LoginForm = () => {
  return (
    <div className="flex flex-col items-center justify-center h-30 bg-red-400">
      <div>HELLO</div>
      <Login />
    </div>
  );
};

export default LoginForm;
